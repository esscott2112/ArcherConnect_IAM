@model IEnumerable<ArcherConnect_IAM.Models.vw_OrgTypeAssignment>

@{
    ViewBag.Title = "Index";
}

<h2>Organizations</h2>
<h3>This is a list of Organizations with thier assigned Organization Type</h3>
<p>
  <!--  @Html.ActionLink("Create New", "Create") -->
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.OrganizationName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OrganizationType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.OrganizationName)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.OrganizationType)
    </td>
    <!--   <td>
           @Html.ActionLink("Edit", "Edit", new { id=item.OrgTypeAssignmentId }) |
           @Html.ActionLink("Details", "Details", new { id=item.OrgTypeAssignmentId }) |
           @Html.ActionLink("Delete", "Delete", new { id=item.OrgTypeAssignmentId })
       </td> -->
</tr>
}

</table>
