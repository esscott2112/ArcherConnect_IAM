@model IEnumerable<ArcherConnect_IAM.Models.vw_EffectiveUserPermission>

@{
    ViewBag.Title = "Index";
}

<h2>Login Effective Permissions</h2>
<h3>A list of the permissions assigned to a login.  More work is needed to apply most restrictive or least restrictive conflict resolution
    logic to complete effective permission calculations in the system.</h3>
<p>
    <!--  @Html.ActionLink("Create New", "Create")-->
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Username)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ElementName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ModuleName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PermissionName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.Username)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.ElementName)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.ModuleName)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.PermissionName)
    </td>
    <!--  <td>
          @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
          @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
          @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
      </td>-->
</tr>
}

</table>
